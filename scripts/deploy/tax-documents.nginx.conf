# Include inside the API server block, ahead of generic storage/static handling.
# This blocks legacy public tax URLs even if an old file is accidentally restored.
location = /storage/tax-documents {
    return 404;
}
location ^~ /storage/tax-documents/ {
    return 404;
}
